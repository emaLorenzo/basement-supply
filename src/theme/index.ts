export const BREAKPOINTS = {
  tabletMin: 550,
  desktopMin: 850,
};

export const QUERIES = {
  mobile: `(max-width: ${BREAKPOINTS.tabletMin}px)`,
  tablet: `(max-width: ${BREAKPOINTS.desktopMin}px)`,
};
